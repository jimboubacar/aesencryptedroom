<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillViewport="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Title (optional, kept for Room demo) -->
        <!-- Title (editable) -->
        <EditText
            android:id="@+id/etTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:autofillHints="Add title"
            android:hint="@string/title"
            android:inputType="textCapSentences|textPersonName"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:imeOptions="actionNext">
            <requestFocus/>
        </EditText>


        <!-- Plaintext input (editable) -->
        <EditText
            android:id="@+id/etPlain"
            android:autofillHints="Write Text"
            android:hint="@string/write_your_plaintext_here"
            android:inputType="textMultiLine"
            android:minLines="3"
            android:gravity="top"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Action buttons for local crypto -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">

            <Button
                android:id="@+id/btnEncrypt"
                android:text="@string/encrypt"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <Button
                android:id="@+id/btnDecrypt"
                android:text="@string/decrypt"
                android:layout_marginStart="8dp"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />
        </LinearLayout>

        <!-- Ciphertext box (non-editable) -->
        <TextView
            android:id="@+id/tvCipher"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:padding="12dp"
            android:background="#EEEEEE"
            android:textColor="#000000"
            android:textIsSelectable="true"
            android:fontFamily="monospace"
            android:hint="@string/encrypted_text_base64_iv_ct_will_appear_here"
            android:text="" />


        <!-- Room demo controls (optional) -->
        <Button
            android:id="@+id/btnSave"
            android:text="@string/save_encrypted_to_room"
            android:layout_marginTop="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <Button
            android:id="@+id/btnLoadLast"
            android:text="@string/load_last_from_room"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/tvResult"
            android:text="@string/result_will_appear_here"
            android:layout_marginTop="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>
</ScrollView>

